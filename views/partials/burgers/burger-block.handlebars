<li>
	{{id}} : {{name}}
	<br>
	{{#if orderUp}}
		<button class="change-order btn btn-primary" data-id="{{this.id}}" data-neworder="{{orderUp}}">Deliver</button>
	{{/if}}

	{{#unless orderUp}}
		<button class="change-order btn btn-danger" data-id="{{this.id}}" data-neworder="{{orderUp}}">Ugh gross, send it back!</button>
		<button class="delete-burger btn btn-warning devour" data-id="{{this.id}}">Eat da Burger!</button>
	{{/unless}}

	
</li>