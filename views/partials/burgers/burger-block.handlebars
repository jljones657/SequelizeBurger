
<li>
	{{name}}
	<br>

	{{#unless order}}
		<button class="change-order btn btn-primary" data-id="{{id}}" data-neworder="{{order}}">Deliver</button>
	{{/unless}}

	{{#if order}}
		<button class="change-order btn btn-danger" data-id="{{id}}" data-neworder="{{order}}">Ugh gross! Send it back!</button>
		<button class="delete-burger btn btn-warning devour" data-id="{{this.id}}">Eat da burger!</button>
	{{/if}}
</li>